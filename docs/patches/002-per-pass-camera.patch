diff --git a/AfxHookSource2/CMakeLists.txt b/AfxHookSource2/CMakeLists.txt
index 1d66528..d86c755 100644
--- a/AfxHookSource2/CMakeLists.txt
+++ b/AfxHookSource2/CMakeLists.txt
@@ -59,6 +59,19 @@ SET(AFXHOOKSOURCE2SNIPPETS_OUTPUTS ${AFXHOOKSOURCE2SNIPPETS_OUTPUTS} PARENT_SCOP
 
 add_definitions(-DGAME_CS2)
 
+# cs2-vr-spectator: OpenXR headers. The loader is opened by explicit path at runtime
+# rather than linked, so only the include directory is needed here.
+if(NOT DEFINED AFXVR_OPENXR_DIR AND DEFINED ENV{AFXVR_OPENXR_DIR})
+    set(AFXVR_OPENXR_DIR_DEFAULT "$ENV{AFXVR_OPENXR_DIR}")
+else()
+    set(AFXVR_OPENXR_DIR_DEFAULT "D:/Dev/cs2-vr-tools/openxr/pkg")
+endif()
+set(AFXVR_OPENXR_DIR "${AFXVR_OPENXR_DIR_DEFAULT}" CACHE PATH "OpenXR SDK root (headers under include/)")
+if(NOT EXISTS "${AFXVR_OPENXR_DIR}/include/openxr/openxr.h")
+    message(FATAL_ERROR "cs2-vr-spectator: OpenXR headers not found at ${AFXVR_OPENXR_DIR}/include/openxr/openxr.h -- pass -DAFXVR_OPENXR_DIR=<sdk root>, or set the AFXVR_OPENXR_DIR environment variable. See cs2-vr-spectator/docs/install.md.")
+endif()
+target_include_directories(${PROJECT_NAME} PRIVATE "${AFXVR_OPENXR_DIR}/include")
+
 #set_target_properties(
 #    ${PROJECT_NAME}
 #    PROPERTIES 
@@ -248,6 +261,11 @@ target_sources(${PROJECT_NAME} PRIVATE
 
 	MirvTime.cpp
 	MirvTime.h
+	MirvVr.cpp
+	MirvVr.h
+	MirvVrMath.h
+	MirvVrXr.cpp
+	MirvVrXr.h
 
     ReplaceName.cpp
     ReplaceName.h
diff --git a/AfxHookSource2/RenderServiceHooks.cpp b/AfxHookSource2/RenderServiceHooks.cpp
index cea3626..ad2a751 100644
--- a/AfxHookSource2/RenderServiceHooks.cpp
+++ b/AfxHookSource2/RenderServiceHooks.cpp
@@ -5,8 +5,10 @@
 #include "RenderSystemDX11Hooks.h"
 
 #include "../shared/AfxDetours.h"
+#include "../shared/AfxConsole.h"
 
 #include "addresses.h"
+#include "MirvVr.h"
 
 #include <stdint.h>
 
@@ -57,7 +59,9 @@ void __fastcall My_Engine2_RenderService_OnClientOutput(void * pUnk0, void * pUn
 
     RenderSystemDX11_EngineThread_BeforeRender();
 
+    if(g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) advancedfx::Message("AFXVR: frame=%i pass=%i OnClientOutput enter\n", g_AfxVrFrameIndex, g_AfxVrPassIndex);
     g_Engine2_RenderService_OnClientOutput(pUnk0,pUnk1);
+    if(g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) advancedfx::Message("AFXVR: frame=%i pass=%i OnClientOutput leave\n", g_AfxVrFrameIndex, g_AfxVrPassIndex);
 
     if(RenderSystemDX11_EngineThread_HasNextRenderPass()) {
 
@@ -96,7 +100,9 @@ void __fastcall My_Engine2_RenderService_OnClientOutput(void * pUnk0, void * pUn
             if(bHooksAvailable) {
                 RenderSystemDX11_EngineThread_BeforeRender();
 
+                if(g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) advancedfx::Message("AFXVR: frame=%i pass=%i OnClientOutput enter\n", g_AfxVrFrameIndex, g_AfxVrPassIndex);
                 g_Engine2_RenderService_OnClientOutput(pUnk0,pUnk1);
+                if(g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) advancedfx::Message("AFXVR: frame=%i pass=%i OnClientOutput leave\n", g_AfxVrFrameIndex, g_AfxVrPassIndex);
             }
         }
     }
diff --git a/AfxHookSource2/RenderSystemDX11Hooks.cpp b/AfxHookSource2/RenderSystemDX11Hooks.cpp
index 73b7c00..ce1c803 100644
--- a/AfxHookSource2/RenderSystemDX11Hooks.cpp
+++ b/AfxHookSource2/RenderSystemDX11Hooks.cpp
@@ -8,6 +8,8 @@
 #include "WrpConsole.h"
 #include "CamIO.h"
 #include "MirvFix.h"
+#include "MirvVr.h"
+#include "MirvVrXr.h"
 
 #include "RenderCommands.h"
 #include "StreamSettings.h"
@@ -60,6 +62,11 @@ extern void ExecuteClientCmd(const char * value);
 
 int g_iRenderContextDebug = 0;
 
+// cs2-vr-spectator: which render pass is being set up. 0 is the main pass, 1..N the extra
+// (mirv_streams) passes. Written on the engine thread only, from the pass loop below.
+int g_AfxVrFrameIndex = 0;
+int g_AfxVrPassIndex = 0;
+
 CRenderCommands g_RenderCommands;
 
 bool g_bEnableReShade = true;
@@ -1913,6 +1920,15 @@ public:
                 if (pRenderTargetViews[0]) pRenderTargetViews[0]->Release();
             }
 
+            // cs2-vr-spectator: the UI is composited here, once the scene is finished.
+            // Whether that happens once a frame or once a pass decides whether the eyes
+            // have the HUD baked into them, which decides how issues #2 and #3 can be
+            // solved at all. Cheap, and silent unless mirv_vr_log armed it.
+            if (g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) {
+                advancedfx::Message("AFXVR: frame=%i pass=%i BeforeUi (render thread)\n",
+                    g_AfxVrFrameIndex, g_AfxVrPassIndex);
+            }
+
             ID3D11RenderTargetView* pRenderTargetViews[1] = {nullptr};
             pDeviceContext->OMGetRenderTargets(1, &pRenderTargetViews[0], nullptr);
             if (pRenderTargetViews[0]) {
@@ -2188,6 +2204,21 @@ void Before_Present() {
 
     if(auto pRenderPassCommands = g_RenderCommands.RenderThread_GetCommands())
     {
+        // cs2-vr-spectator: the point after the UI has been composited. Note it reads
+        // through g_BeforeUiRT, the render target captured at the BeforeUi point, so the
+        // contents by now include whatever the UI drew into it.
+        //
+        // Do not read anything into the queue being non-empty here: HLAE queues its own
+        // work on it too. Which point an eye was actually captured at is reported by the
+        // capture itself, below.
+        if (g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) {
+            advancedfx::Message("AFXVR: frame=%i pass=%i BeforePresent (render thread) queue=%s beforeUiRT=%s swapChain=%s ctx=%s\n",
+                g_AfxVrFrameIndex, g_AfxVrPassIndex,
+                pRenderPassCommands->BeforePresent.Empty() ? "empty" : "has work",
+                g_BeforeUiRT ? "yes" : "NULL",
+                g_pSwapChain ? "yes" : "NULL",
+                g_RenderCommands.RenderThread_GetContext() ? "yes" : "NULL");
+        }
         if(!pRenderPassCommands->BeforePresent.Empty()) {
             ID3D11Resource* pRenderTargetViewResource = nullptr;
             if(g_BeforeUiRT) {
@@ -2203,6 +2234,42 @@ void Before_Present() {
                     pRenderTargetViewResource->Release();
                 }
             }
+            // cs2-vr-spectator: no capture point, so use the swap chain itself.
+            //
+            // g_BeforeUiRT is only ever assigned by CAfxRenderCallbackBeforeUi, and that
+            // is queued in exactly one place: when the scene system announces the view
+            // "CSGOHud". At CS2's main menu the view is called CSGOMainMenu, the name
+            // test fails, nothing is captured, and this callback could never be reached -
+            // which is why a VR session started at the menu showed the runtime's loading
+            // placeholder and hung the game.
+            //
+            // Buffer 0 is the finished frame: Before_Present runs immediately before the
+            // real Present, and in D3D11 the back buffer is always index 0, flip model
+            // included. It also covers loading screens and anything else with neither
+            // view, so what the headset shows does not drop out between menu and map.
+            else if(g_pSwapChain) {
+                // OnBeforePresent silently DISCARDS the queue when it has no device
+                // context, and at the menu it may never have been given one:
+                // RenderThread_SetContext is called from New_ClearRenderTargetView when
+                // g_iDraw reaches 0, which is part of the in-game draw detection. The
+                // device's immediate context is the right one here - we are on the render
+                // thread at Present and the callback only does a CopyResource - and the
+                // in-game path overwrites it again on its next frame.
+                if(!g_RenderCommands.RenderThread_GetContext() && g_pDevice) {
+                    ID3D11DeviceContext * pImmediate = nullptr;
+                    g_pDevice->GetImmediateContext(&pImmediate);
+                    if(pImmediate) {
+                        g_RenderCommands.RenderThread_SetContext(pImmediate);
+                        pImmediate->Release();
+                    }
+                }
+
+                ID3D11Texture2D * pBackBuffer = nullptr;
+                if(SUCCEEDED(g_pSwapChain->GetBuffer(0, __uuidof(ID3D11Texture2D), (void**)&pBackBuffer)) && pBackBuffer) {
+                    pRenderPassCommands->OnBeforePresent(pBackBuffer);
+                    pBackBuffer->Release();
+                }
+            }
         }
     }
 }
@@ -2986,6 +3053,13 @@ public:
     void EngineThread_Prepare() {
         m_RecordingExtraPassesIterator = m_RecordingExtraPasses.begin();
 
+        // cs2-vr-spectator: VR is a second source of extra passes, independent of
+        // recording. Without this the eyes would only render while mirv_streams is
+        // writing ~300 MB/s of TGA to disk.
+        MirvVrXr_EngineThread_Frame();
+        m_VrPassesDone = 0;
+        m_VrPassesWanted = MirvVrXr_WantsPasses() ? 2 : 0;
+
         if (m_Recording) {
             if (m_AutoForceFullReSmoke) {
                 if (!m_Restore_smoke_volume_lod_ratio_change) {
@@ -3012,10 +3086,54 @@ public:
     }
 
     bool EngineThread_HasNextRenderPass() {
-        return m_RecordingExtraPassesIterator != m_RecordingExtraPasses.end();
+        return m_RecordingExtraPassesIterator != m_RecordingExtraPasses.end()
+            || m_VrPassesDone < m_VrPassesWanted;
     }
 
     void EngineThread_BeginNextRenderPass() {
+        g_AfxVrPassIndex++; // cs2-vr-spectator
+        if(g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) {
+            advancedfx::Message("AFXVR: frame=%i BeginNextRenderPass -> pass=%i\n", g_AfxVrFrameIndex, g_AfxVrPassIndex);
+        }
+        AfxVr_OnBeginRenderPass(g_AfxVrPassIndex);
+
+        // cs2-vr-spectator: passes 1 and 2 are the eyes. Their finished back buffer
+        // arrives at BeforePresent, on the render thread, which is where it is copied
+        // into the OpenXR swapchain.
+        //
+        // The count is advanced HERE rather than in EndNextRenderPass, which is what the
+        // recording path does. Advancing at the end means HasNextRenderPass is still true
+        // when the last eye has been begun but not yet ended, so the loop starts one more
+        // pass that renders the whole scene and throws it away - a quarter of the frame's
+        // work. The final pass is still ended properly, by the scene system hook.
+        if (m_VrPassesDone < m_VrPassesWanted) {
+            int eyeIndex = m_VrPassesDone;
+            m_VrPassesDone++;
+            auto & pRenderPassCommands = g_RenderCommands.EngineThread_GetCommands();
+            bool beforeUi = MirvVrXr_CaptureBeforeUi();
+            // cs2-vr-spectator: which frame's pose and timing this pass is being rendered
+            // with, captured here on the engine thread and carried to the render thread by
+            // value. Reading it there instead is a race: the engine thread is often a
+            // frame or two further on by the time the pass is submitted.
+            unsigned long long vrTicket = MirvVrXr_EngineThread_FrameTicket();
+            auto submit = [eyeIndex, beforeUi, vrTicket](ID3D11DeviceContext * pDeviceContext, ID3D11Texture2D * pTexture) {
+                if (g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) {
+                    advancedfx::Message("AFXVR: eye %i captured at %s (render thread)\n",
+                        eyeIndex, beforeUi ? "BeforeUi" : "BeforePresent");
+                }
+                MirvVrXr_RenderThread_SubmitEye(eyeIndex, pDeviceContext, pTexture, vrTicket);
+            };
+            // BeforeUi fires once per pass, immediately before that pass's UI is
+            // composited, so capturing there gives an eye with no HUD and no demo menu in
+            // it. BeforePresent is after, and keeps them. Measured in
+            // docs/experiments/14-when-the-ui-is-drawn.md; mirv_vr_xr ui switches it.
+            if (MirvVrXr_CaptureBeforeUi()) {
+                pRenderPassCommands.BeforeUi.Push(submit);
+            } else {
+                pRenderPassCommands.BeforePresent.Push(submit);
+            }
+        }
+
         auto it = m_RecordingExtraPassesIterator;
         while(it != m_RecordingExtraPasses.end()) {
             it->EngineThread_BeginFrame();
@@ -3041,6 +3159,36 @@ public:
     }    
 
     void EngineThread_BeginMainRenderPass() {
+        g_AfxVrFrameIndex++; // cs2-vr-spectator
+        g_AfxVrPassIndex = 0;
+        if(g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) {
+            advancedfx::Message("AFXVR: frame=%i BeginMainRenderPass\n", g_AfxVrFrameIndex);
+        }
+        AfxVr_OnBeginRenderPass(g_AfxVrPassIndex);
+
+        // cs2-vr-spectator: the main pass is the one image that still has the HUD and the
+        // demo menu composited into it once the eyes stop taking them. It is what the quad
+        // layer carries, so hand it over at BeforePresent - after the UI, deliberately.
+        if (MirvVrXr_WantsPanel()) {
+            auto & pMainPassCommands = g_RenderCommands.EngineThread_GetCommands();
+
+            // cs2-vr-spectator: between the world and the UI. Wiping the finished world
+            // out of the back buffer here leaves the HUD drawing onto transparent black,
+            // so the quad carries a HUD and not a second copy of the world hung in front
+            // of the world. The monitor shows that same HUD-on-black while it is on.
+            if (MirvVrXr_WantsPanelClear()) {
+                pMainPassCommands.BeforeUi.Push(
+                    [](ID3D11DeviceContext * pDeviceContext, ID3D11Texture2D * pTexture) {
+                        MirvVrXr_RenderThread_ClearForPanel(pDeviceContext, pTexture);
+                    });
+            }
+
+            pMainPassCommands.BeforePresent.Push(
+                [](ID3D11DeviceContext * pDeviceContext, ID3D11Texture2D * pTexture) {
+                    MirvVrXr_RenderThread_SubmitPanel(pDeviceContext, pTexture);
+                });
+        }
+
         for(auto it = m_RecordingMainPass.begin(); it != m_RecordingMainPass.end(); it++) {
             it->EngineThread_BeginFrame();
         }
@@ -3563,6 +3711,10 @@ private:
     std::multiset<CStream,RenderPassCompare> m_RecordingExtraPasses;
     std::multiset<CStream,RenderPassCompare>::iterator m_RecordingExtraPassesIterator;
 
+    // cs2-vr-spectator: extra passes owned by the VR session rather than by recording.
+    int m_VrPassesWanted = 0;
+    int m_VrPassesDone = 0;
+
     std::map<std::string, CStreamSettings> m_Streams;
 
     bool m_Shutdown = false;
diff --git a/AfxHookSource2/main.cpp b/AfxHookSource2/main.cpp
index efbea01..9e6e7cb 100644
--- a/AfxHookSource2/main.cpp
+++ b/AfxHookSource2/main.cpp
@@ -21,6 +21,7 @@
 #include "MirvColors.h"
 #include "MirvFix.h"
 #include "MirvTime.h"
+#include "MirvVr.h"
 
 #include "../deps/release/prop/AfxHookSource/SourceSdkShared.h"
 #include "../deps/release/prop/AfxHookSource/SourceInterfaces.h"
@@ -649,6 +650,11 @@ bool CS2_Client_CSetupView_Trampoline_IsPlayingDemo(void *ThisCViewSetup) {
 	float *pViewOrigin = (float*)((unsigned char *)ThisCViewSetup + 0x4a0);
 	float *pViewAngles = (float*)((unsigned char *)ThisCViewSetup + 0x4b8);
 
+	// cs2-vr-spectator: this object is persistent and the render passes write eye poses
+	// into it. Undo the last one before reading, or it is mistaken for the game's own
+	// camera and accumulates frame over frame.
+	AfxVr_BeforeViewSetupRead(ThisCViewSetup);
+
 	int width = *pWidth;
 	int height = *pHeight;
 	float Tx = pViewOrigin[0];
@@ -745,6 +751,16 @@ bool CS2_Client_CSetupView_Trampoline_IsPlayingDemo(void *ThisCViewSetup) {
 		}		
 	}
 
+	// cs2-vr-spectator: this is the base pose the eyes are offsets from. The eyes
+	// themselves are applied by the pass loop, not here - this function runs once per
+	// frame and cannot distinguish the passes.
+	if (AfxVr_AfterViewSetup(ThisCViewSetup, Tx, Ty, Tz, Rx, Ry, Rz, Fov)) {
+		// It wrote the head back through those. Everything that reads the camera once
+		// a frame - the audio listener, the world-to-screen matrix, culling - reads it
+		// from here, and saw only the demo camera while the head lived between passes.
+		originOrAnglesOverriden = true;
+	}
+
 	if (g_S2CamIO.GetCamExport())
 	{
 		CamIO::CamData camData;
@@ -978,6 +994,28 @@ void __fastcall New_CViewRender_UnkMakeMatrix(void* This) {
 	g_WorldToScreenMatrix.m[3][2] = proj[4*3+2];
 	g_WorldToScreenMatrix.m[3][3] = proj[4*3+3];
 
+	// cs2-vr-spectator: hand the original over to MirvVr, which calls it again for each
+	// eye so the client's world-to-screen matrix matches the camera that pass renders
+	// with. Registered here because only this translation unit holds the trampoline.
+	AfxVr_SetMakeMatrix(g_Old_CViewRender_UnkMakeMatrix);
+
+	// cs2-vr-spectator: the engine's own projection, so "what field of view did CS2
+	// actually render" stops being a model and becomes a measurement. [0][0] is
+	// 1/tan(horizontal half-angle), [1][1] is 1/tan(vertical half-angle).
+	{
+		float * proj = (float*)((unsigned char*)This + 0x218);
+		AfxVr_SetProjectionDiagonal(proj[0], proj[5]);
+	}
+
+	// cs2-vr-spectator: is the projection rebuilt once per frame or once per pass, and is
+	// this the same object the view setup trampoline writes into?
+	if (g_AfxVrFrameIndex < g_AfxVrLogUntilFrame) {
+		float * p = (float*)((unsigned char*)This + 0x218);
+		advancedfx::Message(
+			"AFXVR: frame=%i pass=%i MakeMatrix this=%p proj[0][0]=%f proj[1][1]=%f\n",
+			g_AfxVrFrameIndex, g_AfxVrPassIndex, This, p[0], p[5]);
+	}
+
 	g_CampathDrawer.OnEngineThread_SetupViewDone();
 }
 
